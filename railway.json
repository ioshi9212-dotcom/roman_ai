{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "uvicorn app.diagnostic_main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/context_stats_probe",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
